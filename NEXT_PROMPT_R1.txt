项目跑起来下单流程就走不通——加车、登录都 OK，但 POST /api/order/create 直接 500 `cannot start a transaction within a transaction`，评价和支付都依赖这个订单所以也连带挂；PDF 试读接口报 `WinAnsi cannot encode "活"`，pdf-lib 不支持中文，榜单表是空的。

技术栈也偏离了，PROMPT 要 Nuxt 3 SSR 实际是 Vite + Vue SPA + axios 拼出来的，这个一并改一下。

检查修复一下。